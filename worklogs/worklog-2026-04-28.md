
## 2026-04-28 Phase 4 Step A — wiki-build-derived-index.py

**做了什麼**：建立反向索引 build script + 8 unit tests + fixtures + .gitignore 補一行。commit `fda5a7a` push 完畢，Issue #157 留 Step A 收尾留言。

**決策原因**：CLI 解析原用手動 arg 解析，path 值被誤判為 positional args，改用 argparse 解決。

**阻礙踩坑**：CLI flag 解析 bug（rest filter 誤抓 path 值），發現後即修正，全 8 tests pass。

## Phase 4 Step C-prime — source 獨立路由 + 被引用 section

**做了什麼**：新增 `src/pages/wiki/sources/[slug].astro` route（visibility=public only，+281 pages）、`SourcePage.astro`（header/summary/key_points/chapters/quotes）、`ReferencedByArticlesSection.astro`（被引用 section）；補 DerivedFromSection Step B TODO 連結、ArticlePage 傳 visibility；修 SSOT docs/article-derived-from.md。commit `dd30e98` 推上 main。

**決策原因**：原 Step C 假設 source 有獨立路由失誤，Paul 拍板建獨立路由，符合北極星追溯 UX。

**阻礙踩坑**：無新坑；視覺驗收 article→source→article 雙向通，internal source 404 正確，pytest 171 pass，pnpm build 835 pages 0 errors。

---

## 2026-04-28 Phase 4 Step D — prebuild 整合 wiki-build-derived-index

**做了什麼**：package.json scripts 加三條：predev（非 strict）、prebuild（--strict）、wiki:build-derived-index（獨立 CLI）。

**決策原因**：pnpm 原生支援 pre* lifecycle hook，不需額外工具；predev 寬鬆避免 author 改 frontmatter 途中卡 dev 啟動；prebuild strict 確保 production build 乾淨。

**阻礙踩坑**：無。pnpm install 後 lockfile 不動，驗收全通過（835 pages 0 errors / pytest 171 pass / consistency 12 refs）。

## 14:48 — wiki Phase 4 Step G 收尾
- **做了什麼**：三件任務全完成 — SSOT Phase 4 打勾+落地紀錄、結案 worklog、Issue #157 結案 comment（A→G commits 列表）
- **決策原因**：handoff 指定的結案流程，Phase 4 全部 A→G 已驗收
- **阻礙踩坑**：push 時 remote 有新 commit（handoff 檔），git pull --rebase 解決，無衝突
- **commit**：1f8173f（step G）→ 76308d5（done report）；已 push main

## Phase 4 i18n derived_from 同步（2026-04-28）

**做了什麼**：把 9 篇 zh article 的 derived_from（共 25 entries / 10 unique sources）沿用 source slug 同步到 en/ja/zh-cn 翻譯文（27 files）。Commit `2425965` 推上 main。

**決策原因**：Step F 期間刻意跳過翻譯文，本 step 補齊讓 i18n 雙向 UI 完整。同步純機械化，source slug 不翻譯（統一識別碼）。

**阻礙踩坑**：`ja/zh-cn/ai-collab-realtime-translator.md` 是 stub 文件（無 body），缺 trailing newline 導致 wiki_corpus_lib regex 失效。補 `\n` 修復，pre-existing 問題。

**驗收**：pytest 180 / validator 36 articles pass / build-derived-index 10 keys 100 entries / pnpm build 835 pages 0 errors / 視覺驗證 3 語系標題正確。

---

## 2026-04-28 (Cowork) Codex 工程診斷評估 + 三項修復交辦

**做了什麼**：
1. 評估 Paul 委託 OpenAI Codex 產出的 4 份工程診斷報告（XSS / GET secret / hreflang / search index）
2. Cowork 對所有 file:line 重新核實 — 三份可動報告的引用全部準確（remark-wikilinks.mjs:106 / index.js:276 / visitors.js:881 / BaseLayout.astro:63）
3. 開 3 個 GitHub Issue：#187（P0 GET secret + token rotation）、#188（P0 Wiki remark XSS）、#189（P1 hreflang）
4. 產出 H7 規範 handoff：`handoffs/cowork--codex-engineering-fixes-2026-04-28.md`（含 frontmatter status: Draft + Consequences 章節 + F-ID 來源事實清單）
5. 更新 PENDING.md：「待 Code 執行」section 加批次 [>]、觀察期項目加 #11（Codex 常態化 N=1）+ #12（search index 結論偏擱置）

**決策原因**：
- **三項合一個 handoff**：Token rotation 跨 task 依賴（Task A 改完必須 rotate，否則修了等於沒修）；批次部署效率高於分批；Code 收到一份完整 spec 不需切換脈絡
- **P0/P0/P1 排序**：GET secret 是「持續累積暴露」（每次 access log/CDN log 都在記錄）；Wiki XSS 是「隨 ingest 數量放大」（LLM 產 meta.title 不是手寫常數）；hreflang 是 SEO 健康度但非緊急
- **擱置 Codex 報告 #4**：Codex 結論不完整——沒爬到 Wiki 有獨立 `/api/wiki/search` + KV seed pipeline，articles 與 wiki sources 是兩條索引線；articles.json 只收主語系是「翻譯文章曝光策略」的產品決策非 bug
- **修 XSS 用 mdast hast 結構而非 escape 函式**：讓下游 hast 自動處理屬性 escape，新增屬性時不會漏；字串拼接每次都要記得 escape，紀律難維持
- **不直接 PATCH Issue #155**：本批次只做交辦不做完成；等 Code 執行完再由 Cowork 接手寫 worklogs/issue-155-body.md 觸發 sync-dashboard action

**阻礙踩坑**：
- Glob 對 `worklogs/worklog-*.md` 與 `handoffs/code--*.md` pattern 找不到結果（其他更具體的 pattern 找得到），疑似 Glob 對 `*` 在中段路徑時行為異常 → 用 bash `ls` 繞道，已能驗結果
- 今天 worklog 已存在（Code session 完成 wiki Phase 4 五個 commit），需 append 而非覆寫，避免破壞既有 Code 紀錄
- Codex 報告對 paulkuo.tw 治理體系無感（不知道 PENDING/Issue/worklog/憲法 v0.2）→ 評估時必須由 Cowork 翻譯成內部協作流程
- 司法核查發現 Codex 標的 file:line 偶有 ±1-2 行誤差（例如 XSS 標 :106 實際在 :105-106 區間），屬可接受範圍但 Code 收到 handoff 後仍應重新核對

**狀態變更**：
- Issue #187（GET secret）：未識別 → 已開 Issue + handoff，等 Code 執行
- Issue #188（Wiki XSS）：未識別 → 已開 Issue + handoff，等 Code 執行
- Issue #189（hreflang）：未識別 → 已開 Issue + handoff，等 Code 執行
- PENDING.md 觀察期：8/9/10 → 8/9/10/11/12（11 N=1 常態化觀察、12 search index 擱置）

**Smoke Test**：本批次 Cowork 端只交辦不部署，Smoke Test 由 Code 執行 Task A/B/C 後補上，依 handoff §6 統一收尾欄位驗收。

**待 Cowork 收尾**（Code 完成本批次後）：
- 編 `worklogs/issue-155-body.md` 把 Issue #187/#188/#189 結案資訊同步到 #155
- PENDING.md 三條 [>] → [x]
- 評估觀察期項目 #11 是否進入 N=2 計數


## Phase 4.5 Session 1 — Wiki 視覺融合（半融合）commit 1822203

**做了什麼**：
- SourcePage.astro：container 840px、h1 改 `--font-display` + 2.4rem + `--brand-navy`、pillar-dot inline style 對齊 ArticlePage、`source-pillar-badge` 改為 `.source-pillar` + global `.pillar-dot`
- DerivedFromSection.astro + ReferencedByArticlesSection.astro：全 `--color-*` fallback token 替換為 `--text-primary / --text-secondary / --border / --bg-section / --brand-navy` 系統
- ReferencedByArticlesSection.astro：langDisplay `中` → `繁`，對齊 ArticlePage mobile-lang-bar
- global.css / ArticlePage.astro：均未更動（作為 reference）

**決策原因**：Cowork handoff §1 踩坑預警已確認 token 命名系統（`--text-primary` 系列，非 `--color-*`）；pillar-dot 直接複用 global.css line 281 現成 class；max-width 對齊 article-page 840px，不用 `--max-width` 變數（article 本身 840px，非全站 1200px）。

**阻礙踩坑**：無新踩坑。Cowork §1 六條預警全部命中，handoff 已預先修正，照做即可。Build 835 pages / 0 errors，pytest 180 passed。
