# Worklog 2026-04-27

## [wiki-quarantine-classify] fast-path rule 上線

**做了什麼**
在 `scripts/wiki-quarantine-classify.py` 的 `classify()` 加 fast-path：
當 `quarantine.review_outcome` 已設定且 `quarantine.needs_review is False`，
直接回傳既有決議，不再跑 rules。
新增 4 個 pytest 覆蓋：honours / overrides rule / skipped when needs_review=True / skipped when no review_outcome。

**決策原因**
18 筆剩餘 quarantine source 已全數由 wiki-quarantine-apply.py（04-26）
套用人工決議（`review_outcome: keep_internal`, `needs_review: false`），
但 classifier 沒有 fast-path，重跑仍會讓 13 筆落入 `needs_human_review`。
Fast-path 讓 classifier 成為幂等：重跑結果永遠與人工決議一致。

**阻礙踩坑**
無。

**Commits**
- e38cae6 feat(wiki-classify): add fast-path for existing human review decisions
- f3a7f92 test(wiki-classify): 4 unit tests for fast-path rule
- (chore commit: classify output + handoff)

**重跑結果**
```
keep_internal     18
needs_human_review 0
TOTAL             18
```

---

## [wiki-youtube] youtube_blocklist 串接完成 — 2026-04-27

**做了什麼**：把 `data/wiki-ingest-blocklist.json` 的 `youtube_blocklist` 區塊接進 `scripts/wiki-youtube-ingest.cjs`。新增 `loadYoutubeBlocklist()`（fail-open），在 `pullPending()` 和 `triggerIngest()` 各加一個 short-circuit check。黑名單命中時：`triggerIngest` exit 1 + 印 reason；`pullPending` ⊘ log + kvDelete KV pending key。

**決策原因**：Cowork 04-27 已完成 schema 設計（commit 5aa3ba8 + a9cbb1c），Code 只做機械串接，無設計決策。兩個 check 點獨立 load blocklist，避免共享狀態。

**阻礙踩坑**：本地落後 origin/main 7 個 commit（含 Cowork 的 schema 擴充），pull 後才能開始。無其他阻礙。

**Dry-run 驗證**：
- 黑名單 manual trigger（0h3P7Ojn-3o）→ exit 1 + reason ✅
- 正常 manual trigger（dQw4w9WgXcQ）→ exit 0，行為不變 ✅
- pullPending 遇黑名單 → ⊘ + KV key 記錄到 deletedKeys ✅
- Fail-open（JSON parse 失敗）→ 回傳 {} ✅

**Commit**：af07478

---

## wiki-enrich.cjs parser 換 gray-matter（commit bd5072f）

**時間**：2026-04-27 22:18

**做了什麼**：
承接 Cowork handoff `cowork--wiki-enrich-parser-gray-matter-handoff-2026-04-27.md`，把 `scripts/wiki-enrich.cjs` 裡 5 個手寫 YAML helper（splitFrontmatter / yamlGet / yamlStr / buildEnrichmentYaml / writeFrontmatter-string）全部拔除，換成 gray-matter。新增 2 個 helper：`buildEnrichmentObject`（回傳 JS object）+ matter-based `writeFrontmatter`（用 `matter.stringify` 做 round-trip）。`loadConcepts` / `enrichOne` / `runSingle` / `discoverSources` 全部改用 `matter().data`。

**決策原因**：
原手寫 parser `yamlGet` 只抓單行 value，遇 multi-line / list / nested 全失敗。今天 dialogue detector 啟用時就因 `tags` 是 `list[str]` 炸過一次（commit fb3b10c）。先換 parser 再動 Enrichment 邏輯，省力且降低炸點。

**等價性驗證**：
- dry-run：不寫檔，行為等同 ✅
- --force fixture（youtube-j1V_C6qxT20-ai）：body 0 變動，非 enrichment 欄位值等同，格式差異僅 quoting / key 順序（gray-matter 標準行為）✅
- `npm run check:wiki-visibility`：pass ✅
- Astro build：554 頁，無錯誤 ✅

**阻礙踩坑**：
- gray-matter 會把裸 YAML date（`2026-04-10`）解析成 Date object，再序列化成 ISO 格式（`2026-04-10T00:00:00.000Z`）。確認 Astro schema 的 `created` / `updated` 用 `z.coerce.date()`，兩種格式均接受，無影響。
- `enriched_at` 用 `z.string()`，我們寫入時是 JS string（TODAY），gray-matter 保持 string 不轉 Date，安全。
