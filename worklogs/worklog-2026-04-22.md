# Worklog 2026-04-22

## E2.5 Wiki Enrich Prompt 強化（commit 1fada00）

### 做了什麼
執行 cowork--wiki-enrich-e2-review-2026-04-21.md 全部必修項目：
1. **SYSTEM_PROMPT rule #7 擴充**：加入「如主題與 concept 清單完全不相關 → matched 留空陣列，candidates[0].reason 標明無對齊」規則
2. **SYSTEM_PROMPT rule #10 新增**：每個 matched concept 必須在 candidates 首個 reason 附逐字稿原文證據，無據則移除
3. **buildEnrichmentYaml wrong_pillar 偵測**：matched=[] + candidates[0].reason 含「無核心對齊」→ 自動寫入 `wrong_pillar_suspected: true` + `enrichment_notes`，terminal 警告 + log
4. **content.config.ts**：加 `wrong_pillar_suspected: z.boolean().optional()` + `enrichment_notes: z.string().optional()`

### 3 支測試樣本重跑結果（--force）

| 樣本 | 預期 | 實際 | 判定 |
|------|------|------|------|
| youtube-OxJzRU-6tuk（睡眠） | matched=[], wrong_pillar=true | matched=[learning-as-meta-skill, human-judgment-in-ai-era] | ⚠️ partial — Haiku 仍 surface-match |
| youtube-9yvP7PAunYs（Sora） | 移除 builder-mindset + ai-agent-economy | matched=[software-disruption, one-person-team, build-for-models] | ✅ 惡劣 match 已移除 |
| youtube-8pncy425QqQ（群核 AI） | 維持精準不退步 | matched=[ai-embodiment, build-for-models] | ✅ 穩定 |

### 決策原因
Sora + 群核 AI 測試通過，確認 rule #10 逐字稿證據有效壓制 surface-match。
睡眠影片問題：Haiku 的 learning-as-meta-skill 推理（「睡眠是認知的基礎」）是一種間接類比，而非直接論述主軸。這是第二層問題，需 E2.5.1 針對 pillar-mismatch 場景加強 prompt。

### 阻礙踩坑
- wrong_pillar 自動偵測依賴 Haiku 回傳空 matched，但 Haiku 仍找到間接理由 match，detection 未觸發。
- 偵測機制邏輯正確，但 trigger 條件需 Haiku 完全遵守 rule #7，目前合規率約 70%。

### 待辦（交 Cowork 決策）
- 睡眠影片是否需 E2.5.1 補強（加 system prompt 前置陳述：「若 pillar 與 concept 清單完全不同領域，優先留空而非尋找間接連結」）
- 26 支批次中是否有其他 wrong-pillar 案例？建議 Cowork 決定後再批跑


---

### E2.5.1 — 禁止間接類比推理 prompt 修改

**做了什麼**：在 `scripts/wiki-enrich.cjs` SYSTEM_PROMPT rule #7 末尾追加明確禁令，禁止「睡眠→認知→學習→learning-as-meta-skill」這類多步推理鏈。修正 template literal 反引號跳脫問題（`\`` ）。

**決策原因**：E2.5 後睡眠影片仍剩 2 個 matched，根因是 Haiku 用間接類比繞過空陣列規則。加明確範例能讓 LLM 理解哪種推理不合法。

**阻礙踩坑**：追加文字包含反引號，在 template literal 字串中導致 SyntaxError，需跳脫為 `\``。

**驗收結果**：`youtube-OxJzRU-6tuk` matched:[] + wrong_pillar_suspected:true ✅；對照組 `youtube-8pncy425QqQ-ai` matched 無退步 ✅。

**狀態**：commit `3bd2128` push 完成；handoff 回報填寫完畢，等 Cowork 放行全量 batch rerun。

---

## E3 — Concept 頁面生成 | 2026-04-22

### 做了什麼
- 建 `scripts/wiki-concept-candidates.cjs`：掃所有 sources 的 candidates 欄位，統計提名次數，輸出排序清單
- `src/content.config.ts` wikiSchema 加 `paul_perspective: z.string().optional()`
- 新建 6 個 concept 頁（candidates ≥ 2 且不存在）：supply-chain-resilience、antifragility、stakeholder-collaboration、risk-management、world-model、business-model-innovation
- 批次更新 38 個 concept 頁（含 6 個新建 + 32 個既有）：補 paul_perspective、從 matched 重算 source_count 與 linked_from

### 決策原因
- source_count 與 linked_from 基於 matched（已確認連結），非 candidates（提名建議），確保資料品質
- 新建 concept 的 source_count 暫為 0（待後續 ingest 時 sources 更新 matched 後自動累積）
- build 554 pages, 0 errors，commit: 9315904，push: 76ac2b9

### 阻礙踩坑
- 遠端有新 commit（284cee2 → 248e6a3），pull --rebase 後推送成功

### 待辦
- Cowork 確認後跑 `wiki-kv-seed.cjs` 同步 KV
