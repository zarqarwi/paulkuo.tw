# Code Handoff — Issue #182: iPhone/Safari 無法打卡

> ⚠️ **Handoff 狀態標注（依 v4.9 護欄 #14）**
> - 本文件所有「可能原因」為 **假說（hypothesis）**，非 ground truth
> - Cowork 無法直接 D1 查詢，需 Code 本機 `wrangler d1` 先偵察才能確認根因
> - 模型建議：**Sonnet 4.6（medium effort）** — 偵察任務為主，不建議開 Opus

## 背景

- 來源 feedback：#35（2026-04-17 01:16）、#36（2026-04-17 01:17，有截圖）
- 用戶：`U64a0c5109d54a61492f52d16a81326bf`（iPhone Pro Max / iOS 18.7 / Safari 26.3）
- 描述：「無法打卡」（僅 4 字，資訊極少）
- **活動日**：起駕後第 5 天（4/12 起駕）— 活動凍結期仍在

## 目標

1. 偵察該用戶實際狀態，判斷是「前端 UI 誤導」還是「後端 API 真的沒收到」
2. 若是純環境問題（Safari 外部瀏覽器 + LIFF 失敗）→ wontfix + 改善 LINE 引導文案（延後活動後）
3. 若是程式 regression → 提 PR 走 preview 驗收（Level 2），**嚴守活動期間部署門檻**

## Step 0：偵察（必跑，才判斷要不要動程式碼）

### 0-1. 讀截圖

```bash
# 先看截圖確認用戶看到的畫面
open https://api.paulkuo.tw/api/formosa/feedback-image/1776388643760-8625b18b.png
```

### 0-2. D1 查用戶實際狀態

```bash
cd ~/Desktop/01_專案進行中/paulkuo.tw/worker

# 查 users 表
wrangler d1 execute formosa-esg \
  --command="SELECT user_id, display_name, gps_count, survey_done, last_active, first_active FROM users WHERE user_id='U64a0c5109d54a61492f52d16a81326bf'" \
  --config wrangler.toml --remote

# 查最近 GPS 點
wrangler d1 execute formosa-esg \
  --command="SELECT id, latitude, longitude, timestamp, speed, source FROM gps_points WHERE user_id='U64a0c5109d54a61492f52d16a81326bf' ORDER BY timestamp DESC LIMIT 10" \
  --config wrangler.toml --remote

# 查 checkins
wrangler d1 execute formosa-esg \
  --command="SELECT * FROM checkins WHERE user_id='U64a0c5109d54a61492f52d16a81326bf' ORDER BY created_at DESC LIMIT 5" \
  --config wrangler.toml --remote
```

**判讀規則**：
- 若 D1 有 GPS 點且 timestamp 接近 2026-04-17 01:00 附近 → 後端 OK，前端 UI 誤導（可能是 localStorage 顯示問題）
- 若 D1 完全沒資料 → API 真的沒收到 → 往 LIFF / auth / CORS 方向查
- 若 GPS 有但 checkins 沒有 → 只有背景追蹤成功，手動打卡按鈕失敗 → 前端 button state bug

### 0-3. Worker logs

```bash
# Tail Worker logs 看有沒有 401/429/500
cd ~/Desktop/01_專案進行中/paulkuo.tw/worker
wrangler tail --config wrangler.toml --format pretty | grep -E "checkin|U64a0c510"
# （背景跑，等下一次用戶回來測試時觀察；或改查歷史 logs）
```

## 可能原因（依機率）

見 Issue #182 內文，複製如下：

1. Safari 外部瀏覽器沒完成 LINE 登入 → LIFF 未初始化 → checkin POST 401
2. GPS 權限未授權（Safari 較嚴）
3. 打卡按鈕 disabled 狀態殘留（#143 regression？）
4. iOS 18.7 + Safari 26.3 特有相容性問題
5. Rate limit 429

## 具體修復路徑（分情境）

### 情境 A：環境問題（最可能）

若偵察結果是「Safari 外部瀏覽器 + LIFF 未完成」：

- **活動期間不動程式碼**
- PATCH feedback status 為 `wontfix`，admin_note 寫明「請從 LINE 內建瀏覽器打開 mazu.today」
- 活動後再評估是否加「外部瀏覽器 → 引導進 LINE」的落地頁

### 情境 B：真的是 bug（需要改）

若偵察確認 checkin API 真的壞了：

- **P1 但仍須嚴守門檻**：只有「純後端修復 + 零前端副作用」才在活動期間 push
- 前端 UI 修復一律延後到活動後 merge（參考 `fix/formosa-post-event` branch 既有做法）
- 修復走 PR + Cloudflare preview，Paul 手機親驗

## 驗證方式

1. 修復（或判 wontfix）後 → PATCH feedback #35 / #36 status 為 `fixed` 或 `wontfix`
2. Worker log 觀察後續是否還有同 user_id 的 checkin 失敗
3. Cowork session 下一次 triage 會自動讀 status，確認閉環

## 注意事項

- ⚠️ **活動期間部署門檻**（`feedback_activity_period_deploy_bar.md`）：純後台且零體感副作用才能 deploy
- ⚠️ **不要靜默猜測根因**（`feedback_handoff_default_as_hypothesis.md`）：D1 實測優先於程式碼 grep
- ⚠️ **確認完成狀態三態**（`feedback_code_done_tri_state.md`）：commit SHA pushed / local only / uncommitted
- ⚠️ **跨 repo 驗證**（`feedback_cross_repo_deploy_verification.md`）：若改 Worker，deploy 前 GitHub MCP 驗 main
- 如偵察後判為 wontfix，記得在 Issue #182 補 comment + close，並更新 feedback status

## 相關歷史

- Issue #143（closed 4/7）— 打卡按鈕沒反應，類似症狀但根因不同
- Issue #172（closed 4/15）— localStorage 隔離 + fallback 陷阱，已修為未登入顯示「請先登入」
- `feedback_silent_checkin_failure_blindspot.md` — 打卡靜默失敗盲點 memory

🤖 Auto-generated by Cowork feedback pipeline @ 2026-04-17
