# Worklog 2026-04-12

## 完成日誌（最新在上）
- 04-12 01:29 修復 LINE 內建瀏覽器分享連結空白頁 — 移除 crawler regex 中的 `line` (ba6fe22, #166) Code
- 04-12 00:06 changelog zh-Hans push 完成，4 語言版本全部上線 (d028a37) Code

## 待辦快照
### 高優先 🔴
（無）

### 中優先 🟡
（無）

## 待 Paul 執行
（無）

## 技術備忘
- crawler regex `/line/i` 匹配到 LINE 內建瀏覽器 UA（Line/13.x.x），導致真實用戶被當爬蟲回傳 OG-only HTML
- LINE URL preview bot UA 本身包含 `facebookexternalhit`，移除 `line` 不影響 OG 預覽功能
- CDN max-age=3600，部署後最多 1 小時快取過期才完全生效
- zh-Hans rebase 遇到 reject 是因為 remote 已有其他 language 的 commit，pull --rebase 後正常 push
- Cloudflare Pages 的 changelog 是 SSR + JS 動態載入 lang，curl 無法直接驗證 zh-Hans 內容，需用瀏覽器確認
