
## fix/formosa-post-event merge（Formosa ESG 2026 結案）

### 做了什麼
- merge fix/formosa-post-event → main（530b270），解決 3 個 merge conflict（changelog EN/JA + worklog-2026-04-18.md）
- Worker deploy（analytics/reclass endpoint + bot 三軌分類）
- Close GitHub Issues #173/#174/#177/#178/#179，各附驗收說明

### 決策原因
- Changelog conflict：保留 HEAD（status-deployed 是活動結案後正確狀態）
- Worklog conflict：保留 HEAD 完整紀錄 + 加入 fix branch 的 Code 視窗一行
- #177/#178 close with comment「已加邏輯待實機驗證」，因 Vivo 背景行為無法自動化驗收

### 阻礙踩坑
- commit-msg hook 擋下 merge commit（worker/src/index.js 改動需標注影響範圍），補 [影響: Formosa ESG] 後通過

---

### 22:xx — 加入台灣供應商 SB 253 FAQ 區塊

**做了什麼：** 把 Paul 提供的 5 問 FAQ（台灣供應商 × SB 253 影響、時間表、資料要求、不配合後果、第一步行動）加到 `california-sb253-supply-chain.md` 正文最後、延伸閱讀之前，四語言版本同步更新（zh-tw / en / ja / zh-cn）。commit `bc3d34e`（rebase 後）。

**決策原因：** FAQ 是補充性 actionable 資訊，放在「延伸閱讀」前讓讀者有實際行動出口；來源標注（[1]~[10]）在 MD 格式不必要，直接移除保持乾淨。

**阻礙踩坑：** push 時遠端有新 commit（cron 或其他操作），pull --rebase 解決，無衝突。

---

## JSON-LD @id 引用修正（AI Ready 評分 +8~10 分）

**時間：** 2026-04-21 14:25
**Commit：** b5f604d

### 做了什麼
- `ArticlePage.astro`：移除 inline `personSchema`/`websiteSchema`，改為 `{ "@id": "https://paulkuo.tw/#person" }` / `{ "@id": "https://paulkuo.tw/#website" }` 純 @id 引用
- `BlogPage.astro`：同上（發現 BlogPage 也有同樣問題，一併修正）
- 確認 `/blog` 頁面已有 CollectionPage schema（問題 2 已解決，無需額外改動）

**決策原因：** Chat session 診斷 JSON-LD 層得 12/25，主因是每頁重複 inline Person entity 觸發「No duplicate entities」扣分。About 頁作為 Person entity 的首次宣告根節點，inline 保持不變。

**阻礙踩坑：** `ArticlePage.astro` 已有 `import { PILLAR_MAP, SITE } from '../config'`，移除 personSchema import 時注意不要重複 import SITE。

### Smoke Test
- ✅ `npm run build` 無 error（548 pages）
- ✅ 文章頁 @graph nodes：4 個（Person/WebSite 都是純 @id 引用，BlogPosting + BreadcrumbList 完整）
- ✅ blog 頁 @graph nodes：4 個（Person/WebSite @id 引用，CollectionPage + ItemList 完整）
- ✅ `wrangler deploy` 成功

**待辦：** 等 Cloudflare 重建（2-3 分鐘）後跑 eval worker，確認分數從 12/25 → 目標 20-22/25

## 16:44 — fix: .well-known/ 路由修正（MCP+A2A AI Ready eval）

**做了什麼**：在 `public/_redirects` 最上方加 `.well-known/mcp.json` 和 `.well-known/agent-card.json` 的 200 rewrite，在 `public/_headers` 加對應 `Content-Type: application/json`。commit cc1df59，push 完成。

**決策原因**：AI Ready eval MCP+A2A 層得 0/25，兩個 endpoint 回傳首頁 HTML 而非 JSON；Cloudflare Pages 路由優先順序問題，顯式 200 rewrite 放最上方即可修正。

**阻礙踩坑**：無。`dist/.well-known/` 有檔案，只需路由層修正。

**狀態**：等待 Cloudflare 重建（約 2-3 分鐘）。Smoke test 待跑。

## 19:44 — Smoke Test 通過（.well-known/ 修復完成）

**Smoke Test**
- ✅ `/.well-known/mcp.json`：回傳 `{"name":"paulkuo.tw",...}` JSON（非 HTML）
- ✅ `/.well-known/agent-card.json`：回傳 `{"name":"paulkuo.tw",...}` JSON（非 HTML）

**決策補充**：v1 修法（`_redirects` + `_headers`）無效，因為 Cloudflare Workers Assets 不處理 `_redirects`；v2 改用 Cloudflare Pages Functions（`functions/.well-known/*.json.js`）直接攔截路由並回傳 JSON，才真正生效。commit f404682。

**下一步**：重跑 AI Ready eval，預期 MCP+A2A 0/25 → 25/25。

---

## 20:xx — AI Ready eval 確認 90/100（Cowork Session 結案）

**做了什麼**：跑 AI Ready GitHub Actions eval（`.github/workflows/ai-ready-opt.yml`），確認本輪修復成果。

**最終分數**：
| 層 | 分數 | 滿分 | 變化 |
|---|---|---|---|
| llms.txt | 25 | 25 | ✅ 不變 |
| JSON-LD | 17 | 25 | +5（inline→@id 引用修正） |
| MCP + A2A | 25 | 25 | +25（Pages Functions 修復 .well-known/ 路由） |
| AI Comprehension | 23 | 25 | ✅ 不變 |
| **總分** | **90** | **100** | **+25（65→90）** |

**決策原因**：
- JSON-LD +5：ArticlePage.astro + BlogPage.astro inline personSchema/websiteSchema → @id 引用，消除「No duplicate entities」扣分。commit `b5f604d`
- MCP+A2A +25：Cloudflare Workers Assets 不服務 .well-known/ 點目錄，_redirects 對 Workers 模式無效，改用 Pages Functions（`functions/.well-known/mcp.json.js` + `agent-card.json.js`）攔截路由直接回傳 JSON。commit `f404682`

**阻礙踩坑**：
- wrangler secret 上傳到錯誤 worker（根目錄 wrangler.jsonc 覆蓋子目錄設定），需加 `--config wrangler.toml` 指定
- _redirects 方案失效，因 Workers+Assets 架構與純 Pages 架構路由處理不同

**殘餘缺口（下一輪）**：
- JSON-LD -8：/articles 301 redirect 到 /blog，eval 不追蹤 redirect 所以看到 0 schema；Article property_completeness 也是 0（無文章頁被抽樣）
- AI Comprehension -2：Q3「content pillars 數量」可能與 llms.txt 描述不符（待確認）

**狀態**：本輪結案。Issue #155 已更新。

---

## 22:xx — Wiki Enrichment CLI E1 完成（Issue #157）

**做了什麼**：
- 新建 `scripts/wiki-enrich.cjs` v0.1（Haiku 4.5 驅動）
- `src/content.config.ts` wikiSchema 加 enrichment optional 欄位
- `package.json` + lock 加 `@anthropic-ai/sdk ^0.90.0`
- dotenv loader 修正空值覆蓋邏輯（相容 Claude Code shell 環境注入空 ANTHROPIC_API_KEY）
- commit `eec404d`，push 完成

**dry-run 驗收結果（youtube-8pncy425QqQ-ai，空間智能主題）**：
- ✅ 合法 JSON，所有欄位齊全
- ✅ summary ~230 字（略低於 280-320 規格，Haiku 品質可接受）
- ✅ key_points 5 條、quotes 5 條、chapters 8 段
- ✅ concept_links.matched：ai-embodiment / build-for-models / human-ai-collaboration 等 5 個命中
- ✅ concept_links.candidates：4 個新 concept 候選（spatial-intelligence-technology 等）
- token 消耗：in 10,242 / out 2,340（成本約 $0.002/支）

**決策原因**：
- dotenv loader 原條件 `!(key in process.env)` 不覆蓋空值；Claude Code 環境會注入空的 ANTHROPIC_API_KEY，改為 `|| process.env[key] === ''` 解決
- 不 commit .env（含 secret），API key 由 Paul 在本機維護

**阻礙踩坑**：
- node_modules 含大量空格副本（Finder 複製殘留），導致 npm install 失敗 → 清除後解決

**E1 交付範圍**：dry-run 可跑、concept alignment 有命中、schema 更新。
E2（批次模式）、Pass 2 候選寫檔、Sonnet fallback 等留下一個 session。

---

## Wiki Enrichment E2 批次完成（commit `8741187`）

**做了什麼**：執行 wiki-enrich E2 全流程。修 enriched_at/enriched_by 引號 bug（Astro z.string() 驗證失敗）；改 3 條 prompt（summary 字數 280-320、matched 只列核心、slug 命名規則）；實作 --batch/--force/--type/--pillar flag；批次處理 29 支 YouTube source，✓ 26 / ✗ 3（空 stub）。Build 548 頁全過。

**決策原因**：依 handoff cowork--wiki-enrich-e2-handoff-2026-04-21.md 順序執行。source_type filter bug 發現 getnote 系列無 raw_source_type 欄位，修正為不 default 以防誤撈。

**阻礙踩坑**：3 支失敗為空 stub（< 900 bytes 無逐字稿），非 CLI bug；getnote 誤撈需修 discoverSources()。
