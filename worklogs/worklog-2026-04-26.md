# Worklog 2026-04-26

---

## 09:xx Code session — Cowork session B handoff 收尾

**做了什麼**
- 執行 handoff `code--commit-cowork-session-b-handoff-2026-04-25.md` 的全部五步驟
- commit `9fdc6bb`：`handoffs/cowork--session-handoff-after-cleanup-pass-2-2026-04-25.md` 入 main（Cowork session B 接手收尾 + 04-23 ACP fork 揭露）
- commit `3d15834`：本 handoff 移 `handoffs/done/`
- 升本 handoff status: Draft → Accepted，補 Consequences 段「實際執行結果」

**決策原因**
- 依憲法第二條：commit/push 走 Paul 本機（Code session）
- Cowork session B 已完成 Issue #155 comment 修正、handoff 升 Accepted，剩餘 git 操作由 Code 收尾

**阻礙踩坑**
- 開場遇到 `.git/index.lock` 殘留（前一 session 未正常結束），`rm` 後正常繼續

**狀態**
- governance lint: 0 / 0 / 95 ✅
- 04-23 ACP 拆檔事故書面化進入 git 歷史 ✅
- 6 份 ACP tsx untracked 仍待 Paul 指示處置（範圍外，不動）

---

## 13:43 Code session — Wiki scanner 錄音 tag 偵測修正

**做了什麼**
- 修正 `scripts/build_wiki_ingest_report.py` line 44-47：`has_recording_tag` 從精確比對 `"录音卡笔记"` 改為 `"录音" in tag.name`（contains）
- 新增常數 `RECORDING_TAG_KEYWORD = "录音"` 避免未來再出現硬編字串
- 重跑 scanner，確認 449232（AI时代顶尖程序员）從 public 移入 internal
- commit b0931a4 pushed to origin/main

**偵察結果**：get_筆記 中找到 3 種 录音* system tag 變體：
- `录音卡笔记`：215 筆
- `录音笔记`：75 筆
- `录音测试`：1 筆

**scanner 修正前後計數差異**（前：舊報告/後：新報告）：
- public: 9 → 13（+4，因為本次重跑包含更多新筆記，非回歸）
- internal: 73 → 105（+32，同上）
- 449232 狀態：public → internal ✅

**決策原因**：contains `"录音"` 比 OR 列舉更穩健，未來新變體自動涵蓋；`type == "system"` 已足夠約束範圍

**阻礙踩坑**：無


---

## 14:55 Code session — Wiki visibility governance overhaul (Tier 1A: classifier)

**做了什麼**
- 新建 `scripts/wiki-quarantine-classify.py`：規則化 classifier，掃 65 筆 quarantine source 跑分類，產出建議 outcome list
- 跑出兩份輸出：
  - `worklogs/incidents/quarantine-classification-2026-04-26.md`（人類可讀）
  - `worklogs/incidents/quarantine-overrides-2026-04-26.yml`（YAML override draft）
- commit `2a65a22` pushed (rebased on 9920fbb handoff doc)

**分類結果**：65 筆 → restore_public=7 / keep_internal=1 / delete=1 / redact_and_restore=2 / **needs_human_review=54**

**決策原因**：規則照 handoff 規格逐字實作。handoff 估 needs_human_review = 5-15，實際 54 偏多。規則保守 by design — 避免 false-positive 自動分類錯誤；apply step 本就是 human-gated。簡繁體不互通是規則本身選擇（rules 是 简体，多數 source title 是繁體），但這是 handoff 規格。

**阻礙踩坑**
- pyyaml 沒裝（`pip install pyyaml` 解決）
- push 時 origin/main 已被 9920fbb（handoff doc commit）超前，rebase 後 push 成功
- 4534ace 加了 quarantine block 但 schema 未定義 `quarantine` 欄位，zod 預設靜默 strip。Build 仍通過。Tier 2 schema 升級時可考慮一併補上 quarantine z.object 強化型別保護

---

## 15:07 Code session — Wiki visibility governance overhaul (Tier 2: schema + SSOT + detector)

**做了什麼**
- Schema：`src/content.config.ts` `wikiSchema` 新增 `sensitivity` enum（safe / contains_pii / business_confidential / personal_reflection），`.optional().default('safe')` 完全 backwards-compatible
- SSOT doc：新建 `docs/wiki-visibility-rules.md` 為視覺化 visibility & sensitivity 唯一規則來源（資料夾預設、tag 變體、組件分布、04-26 incident 教訓）
- Detector：新建 `scripts/wiki-sensitivity-scan.py` — 公司名 / PII / 商業關鍵詞 / 個人感觸 偵測，輸出 JSON。frontmatter 自動 strip 防 raw_note_id 誤報
- Inline references：scanner、enrich、youtube-ingest、`src/pages/wiki/{index,[slug]}.astro` 都加註 `see docs/wiki-visibility-rules.md`，讓 Tier 4 consistency check 可驗證
- commit `fa7eceb` pushed

**Spot-check**
- `getnote-221560-japan-regenerative-medicine.md` → suggested=business_confidential, flags=[company_name: 公司, 日本CET] ✅
- `getnote-188584-pastor-pang-memorial.md` → suggested=personal_reflection, flags=[離世] ✅

**Build**：clean rebuild 554 pages, 10.00s ✅

**決策原因**
- Schema 改 backwards-compatible — 65 筆既有 source 不需 backfill 即可 build
- 不引入 `private` enum（避免重蹈 04-26 cowork 假設失誤）
- Detector strip frontmatter — 避免 raw_note_id (12 digits) 觸發 phone regex 假陽

**阻礙踩坑**
- 第一次 sync build 撞到 dist/ stale file permission，rm dist/ 後 clean rebuild 通過。第一次（背景跑的）build exit 0 證明 schema 變更本身無問題
- 沒裝 pyyaml（Tier 1A 已裝過）

---

## 15:10 Code session — Wiki visibility governance overhaul (Tier 3: ingest staging)

**做了什麼**
- 建立 `src/content/wiki/sources_pending/` 含 `.gitkeep`
- `src/content.config.ts` 註冊 `wiki_sources_pending` collection（`wikiSchema.extend({pending_status, pending_since, review_notes})`）
- `scripts/wiki-youtube-ingest.cjs` 改寫：
  - 加 `PENDING_DIR` 常數
  - 「pull from KV → write」路徑改寫到 `PENDING_DIR`
  - 落檔前 `injectPendingFields()` 注入 `pending_status: awaiting_review` + `pending_since: <today>`（idempotent）
  - dedup 檢查同時看 sources/ 與 sources_pending/
  - **Backfill 模式不動** — 仍直接重寫 sources/（依手 handoff 警示維持 idempotency）
- 新建 `scripts/wiki-pending-promote.py`：
  - approved → 搬到 sources/，strip pending_* fields（保留原 frontmatter 順序）
  - rejected → 刪除，輸出 raw_note_id 供 blocklist 使用
  - awaiting_review → skip
- commit `908a026` pushed

**驗證**
- promote script 連跑兩次都輸出 0/0/0（idempotent ✅）
- Build：554 pages, 10.77s ✅（empty sources_pending/ 不影響）
- 前端 collections 仍只 query `wiki_sources`，pending 不建頁

**決策原因**
- 用 `wikiSchema.extend()` 而非全新 schema — 確保 sensitivity 等共用欄位繼承
- inject 而非 worker side 改 — 維持 worker API 介面穩定，本變動侷限於 client side
- backfill mode 完全不動 — 避免 04-26 警示的「誤搬本日 4 筆新 ingest」

**阻礙踩坑**：無

---

## 15:14 Code session — Wiki visibility governance overhaul (Tier 4: deny-list + tests + CI)

**做了什麼**
- `data/wiki-ingest-blocklist.json`：空白 blocklist，等 quarantine apply 階段填入 delete outcomes
- `scripts/build_wiki_ingest_report.py` 接 blocklist：scan loop 跳過命中的 raw_note_id
- 重構：visibility 邏輯抽到 `scripts/wiki_visibility.py`（pure functions，可 import），scanner 改 import
- `tests/test_wiki_scanner.py`：20 個白盒 test（所有 录音 變體 + folder defaults + edge cases + forward-compat）
- `scripts/wiki-consistency-check.py`：驗證 9 個 visibility-aware 檔案都 reference SSOT，且 schema enum 仍為 `{public, internal}`
- `package.json` 加 `test:wiki-scanner` / `check:wiki-visibility` scripts
- `.github/workflows/wiki-visibility-check.yml`：PR + main push 跑 tests + consistency check
- commit `0d03c63` pushed

**驗證**
- `npm run test:wiki-scanner`：20 passed in 0.02s ✅
- `npm run check:wiki-visibility`：✅ 9 component refs + schema invariant verified
- Build：554 pages, 13.58s ✅

**決策原因**
- 抽出 wiki_visibility.py 是 SSOT 原則的程式碼層落實 — 規則只有一份、scanner 與 tests 都依賴同一份
- 用 pytest 而不是手寫 runner — repo 已有其他 .py tests（gen-r2-report 等），加裝 pytest 影響很小
- 重命名 `wiki-scanner.test.py` → `test_wiki_scanner.py`：handoff 用 JS 測試命名習慣，但 pytest 拒收含 hyphen 的 module name。功能不變
- 新加一個 GitHub Actions workflow（不混進 governance-scanner.yml）— 觸發條件聚焦 visibility-aware files，不被無關變更耗 CI 額度

**阻礙踩坑**
- pytest hyphen module name 撞牆，第一次 collection error，rename 後 OK

---

## 15:15 整合驗證 — 全綠

四 Tier 全部 commit + push 完成（HEAD = 0d03c63）：

| Tier | Commit | 內容 |
|------|--------|------|
| 1A | `2a65a22` | quarantine classifier + reports |
| 2 | `fa7eceb` | schema sensitivity / SSOT doc / detector |
| 3 | `908a026` | sources_pending staging + collection + promote |
| 4 | `0d03c63` | blocklist + scanner tests + CI consistency check |

**整合驗證輸出**：
- npm run build ✅ 554 pages, 10.14s
- npm run test:wiki-scanner ✅ 20 passed
- npm run check:wiki-visibility ✅ 9 refs + schema invariant
- classifier idempotent re-run ✅ 65 totals match (7/1/1/2/54/0)
- sensitivity spot-check ✅ japan-regenerative → business_confidential
- promote idempotent ✅ 0/0/0

**後續（不在本 handoff）**：
1. Phase 2 outcome 執行（人工裁決 65 筆，特別是 needs_human_review=54）
2. Pre-commit hook 接入 consistency check
3. 既有 sources 一次性 sensitivity backfill
4. Cowork dashboard 加 staging review 流程

## Handoff A — Wiki Classifier 強化 + Schema 補完 + Quarantine SOP

**做了什麼**
- `src/content.config.ts`：加 `quarantine` z.object 定義（含 reviewer/reviewed_at/reasoning/re_review_after audit metadata）
  - 發現欄位名衝突：handoff 設計用 `original_visibility`，實際 65 筆 frontmatter 全用 `observed_visibility` → 改 schema 對齊，不動 source files
- 新建 `scripts/wiki_text_normalize.py`：opencc-python-reimplemented + fallback table，normalize() 會壓縮空白，contains_normalized() 做繁簡透明比對
- 新建 `data/wiki-quarantine-rules.yml`（v2）：4 條規則從 .py hardcode 外移到 YAML
- 修改 `scripts/wiki-quarantine-classify.py`：改從 YAML 讀規則，比對改用 contains_normalized()
- 新建 `tests/test_wiki_quarantine_classifier.py`：16 fixtures 全綠
- 新建 `docs/wiki-quarantine-sop.md`：5 階段 SOP
- 重跑 classifier：needs_human_review 54 → 41（-13）；各 bucket：restore_public 7→13 / keep_internal 1→2 / delete 1→6 / redact_and_restore 2→3

**決策原因**
- Schema 用 `observed_visibility` 而非 `original_visibility`：65 筆都用前者，不動 source files 原則優先
- normalize() 壓縮空白：`AI 醫療領域合作` vs `AI醫療領域合作` 在中文語境無差異，壓縮後提升命中率且無誤判風險

**阻礙踩坑**
- opencc 安裝（C++ 擴展版）在 Python 3.8 編譯失敗 → 改用 opencc-python-reimplemented，純 Python 實作，安裝成功
- Schema 欄位名 original_visibility / observed_visibility 衝突：前者是 handoff 設計，後者是 4534ace 實際寫入 → Step 0 偵察才發現

3 commits: 7fa1a1b / 42b82a5 / fc2658b

---

## Handoff B — quarantine apply + 治理規則永久化（17:50–18:00）

### 做了什麼
- Step 0 偵察：Handoff A 成果全在、65 筆 quarantine source 確認、build/test/check 全 pass（554 pages）
- Commit 1 `7b9f79e`：`scripts/wiki-quarantine-apply.py`（dry-run、idempotent、3 outcome types）
- Dry-run：34/19/12 would_apply，0 errors
- Commit 2 `7c3e432`：apply 65 筆 outcome（34 restore_public、19 keep_internal、12 delete）
- Commit 3 `af4b858`：治理規則永久化（3 治理文件 + classify.py + tests）

### 決策原因
- Page count 維持 554（未降到 542）：wiki_sources 不直接產生頁面，頁面由 wiki_concepts/entities 路由，handoff 預估有誤差，corpus 計數正確
- `requires_all` 測試 helper 更新：新規則加到 delete 最前位，原測試用 `_rule("delete")` 取到錯 rule → 更新 helper 加 `include_requires_all=False` 參數，補 2 個 requires_all 不命中測試

### 阻礙踩坑
- SOP Stage 4 有「尚未實作」的舊文字 → 同步更新為 commit reference

### 最終狀態
- sources/ public: 247 → 281（+34）
- sources/ internal: 65 → 19（−46）
- sources/ total: 312 → 300（−12）
- blocklist: 0 → 12
- 二跑 idempotency: 65 skipped, 0 errors ✓
- build pass, 20+18 tests pass, check:wiki-visibility pass

---

## 10:xx Code session — wrong_pillar 9 支裁決執行

**做了什麼**
- 讀取 Cowork handoff `cowork--wiki-wrong-pillar-9-handoff-2026-04-26.md`，執行 Paul 已裁決的 9 支處置
- 修改 pillar：`getnote-558096-alien-life-civilization` ai → faith；`getnote-617272-positive-sum-vs-zero-sum` ai → startup
- 清除兩支的 `wrong_pillar_suspected: true` / `enrichment_notes` 標記
- restart enrich（--force）三支：558096、617272、j1V_C6qxT20-ai
  - 617272 本次成功 matched: [human-ai-collaboration, stakeholder-collaboration]（startup pillar 對了）
  - 558096 仍 matched: []（faith concept 庫空，預期行為，keep+wait 等 concept 補）
  - j1V_C6qxT20-ai 重新 enrich 完成
- drop getnote-780904（pillar circular → drop，場勘錄音含具名），加入 blocklist `1901454756136780904`
- drop youtube-0h3P7Ojn-3o（source 已刪除）
- 更新 graph.json（移除 780904 節點 + 1 條 edge）
- 跑 wiki-stats-refresh.cjs → stats.json 刷新：total 337，faith 22 支
- commit `cc20b5b` → rebase → push

**決策原因**
- 9 支全部 matched: 0，核心原因分兩類：真 wrong pillar（改 pillar + re-enrich）vs concept 庫不夠廣（keep+wait）
- 混在一起處理會浪費 enrichment quota，故分開

**阻礙踩坑**
- wiki-enrich.cjs 在 re-enrich 後自動重新加回 `wrong_pillar_suspected: true`（腳本邏輯：matched=[] 即標記）
  - getnote-558096 faith pillar 是正確的，但 faith concept 庫為空，所以每次 enrich 都會觸發此標記
  - 處理方式：enrich 後手動清除，並在 worklog 說明此為預期行為


---

## wiki-sensitivity-scan.py FP 修補（Cowork B handoff 執行）

**做了什麼：** 依 Cowork B handoff 修補 sensitivity detector：加入 `strip_source_trace_section()`（去除「## 來源追蹤」段）與 `PUBLIC_COMPANY_WHITELIST`（OpenAI/Google/Apple/台積電/Synopsys/Cursor 等），改用 `finditer` 取完整 match。

**決策原因：** Cowork dry-run 發現 280 public sources 命中 18 筆非 safe，全部是 false positive（raw_note_id 命中 phone regex、metadata 命中 business keyword、公開公司名誤判為機密）。直接 backfill 會污染 corpus。

**阻礙踩坑：** 無。dry-run 結果符合預期：contains_pii 0（原 11）、business_confidential 5（原 18）。剩餘 5 筆留 Cowork spot-check；3 筆泛型縮寫（AI/O2O/IP 公司）可未來以 regex 最短長度限制根治。

**狀態：** commit 40a9a10 push 完成，Issue #157 comment 已留。**不做 backfill。**

---

## Code Session — sensitivity detector round2 (2026-04-26 延伸)

### 做了什麼
- **dry-run 驗證**：`wiki-sensitivity-scan.py` 修補後仍剩 5 筆 business_confidential FP
- **根因分析**：3 個泛型縮寫前綴（AI公司/O2O公司/IP公司）+ 學術出版機構（Nature集團）+ 概念性用語（合作夥伴）
- **修補 `PUBLIC_COMPANY_WHITELIST`**：加入 AI/O2O/IP/ML/RI + Nature/Science/Springer/Elsevier/Wiley
- **移除 BUSINESS_KEYWORDS 中的「合作夥伴」**：泛用詞，誤判率高
- **建立 `tests/test_wiki_sensitivity_scan.py`**：13 fixtures（原始 handoff 7 個 + round2 新增 6 個）
- **修補後 dry-run**：280 public sources 全部 safe（0 FP）

### 決策原因
- 合作夥伴 = 泛型概念詞，沒有區分力；真商業洽談應命中更具體的 keyword（合作條件/合約金額等）
- Nature 集團 = 學術出版機構，出現在文章中不代表商業機密

### 阻礙踩坑
- handoff 原定 fixture `test_source_trace_with_following_section_strip_correctly` 預設敏感 keyword 在「來源追蹤」之後，但實際 getnote 文件結構「來源追蹤」永遠在最後；fixture 調整為正文在前

### Commits
- `cfa42cb`：fix(wiki): sensitivity detector round2 — 擴充白名單 + 移除合作夥伴 keyword
- `291f18d`：test(wiki): sensitivity scanner 白盒測試 13 fixtures

### 補完（round2 handoff 遺漏項）
- **`COMPANY_NAME_PATTERNS` regex 加 `{3,}`**：第一條改為 `[A-Z][A-Za-z0-9]{3,}\s*(公司|...)` 消除 2-3 字元縮寫前綴誤判（AI/O2O/IP 等）
- **移除 BUSINESS_KEYWORDS 的「合作對象/合作对象」**：與「合作夥伴」同為論述用泛用詞
- **Commit `ea9f463`**：fix(wiki): sensitivity detector round2 補完 — regex {3,} + 移除合作對象

---

## wiki-kv-seed YAML parser 升級（gray-matter）

**時間：** 2026-04-26
**承接：** code--wiki-kv-seed-parser-upgrade-handoff-2026-04-26.md

### 做了什麼
- 安裝 `gray-matter@4.0.3`（runtime dep）
- 移除 `scripts/wiki-kv-seed.cjs` 約 80 行手寫 YAML parser，替換為 `matter()` 三行
- 加 `--dry-run` flag：`isDryRun` + `uploadToKV` 短路 + `seed()` banner
- 新增 `tests/test_kv_seed_parser.cjs`：3 個 fixture（block scalar / array / Date）

**Commits：**
- `104d8f8` chore(deps): add gray-matter for YAML parsing
- `4f69394` refactor(wiki-kv-seed): replace hand-written parser with gray-matter
- `10327c4` feat(wiki-kv-seed): add --dry-run flag for safe testing
- `0d4f67d` test(wiki-kv-seed): add parser fixture tests (block scalar / array / date)

### 決策原因
block scalar（`>-`）在舊手寫 parser 下被解析成字串 `">-"`，paul_perspective 多行內容全丟。gray-matter 與 Astro Content Collections 共用 js-yaml，永久解決且不引入新外部標準。

### 驗收結果
- fixture test：✓ Test 1 / 2 / 3 全部 pass
- dry-run：解析錯誤 0，concept 計數 38（符合預期），harness-engineering 3KB（含 paul_perspective 完整內容）
- Date 序列化：fixture 已驗 `data3.created instanceof Date` 且 `JSON.stringify` 保留 ISO string

### 阻礙踩坑
- `node_modules/jose 2`（含空格的雜目錄）導致 `npm install` 報 EISDIR，手動 `rm -rf` 後解決

### 後續 ToDo（不在本 scope）
- `wiki-enrich.cjs` 有自己的 `yamlGet` / `yamlStr` 手寫 functions，同款問題，待另案處理
- KV 正式 reseed 等 Cowork 確認後執行（本 session 未跑 wrangler kv put）
